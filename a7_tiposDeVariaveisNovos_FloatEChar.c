#include <stdio.h>

int main(){
    int inteiro = 100;      // inteiros positovos e negativos
    float decimal = 10.5;   // faz uso de '.' e não ','
    char caracter = 'a';    // qualquer tipo de caracter n apenas letras

    printf(" %d \n %f \n %c \n", inteiro, decimal, caracter);

    return 0;
}