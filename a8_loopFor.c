#include <stdio.h>

int main(){

    for(int contador = 1; contador <= 10; contador++){ //padrão: for(inicialização; condição; incremento){ do; }       Atenção no usos dos ";"
        printf("Teste\n");
    }
    return 0;

}

// Incrementação: 'i++' é igual a 'i += 1' que é igual a 'i = i + 1'
// dá mesma forma pode ser ultilizado i-- 